import React from 'react';
import '../css/Profile.css';

const Profile = () => {
  const temp = 'pro';
  return (
    <div id={temp} className="profileScreen">
      Profile
    </div>
  );
};

export default Profile;
